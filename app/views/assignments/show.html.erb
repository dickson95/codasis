<p id="notice"><%= notice %></p>

<p>
  <strong>User:</strong>
  <%= @assignment.user.try(:name) %>
</p>

<p>
  <strong>Rol:</strong>
  <%= @assignment.rol.try(:name) %>
</p>

<%= link_to 'Edit', edit_assignment_path(@assignment) %> |
<%= link_to 'Back', assignments_path %>
