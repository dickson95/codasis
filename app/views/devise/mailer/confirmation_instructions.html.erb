<p><%= t('.greeting', :recipient => @resource.email, :default => "Welcome #{@resource.email}!") %></p>

<p><%= t('.instruction', :default => "Puede confirmar su cuenta de correo electrónico a través del siguiente enlace:") %></p>

<p><%= link_to t('.action', :default => "Confirmar mi cuenta"),
         confirmation_url(@resource, :confirmation_token => @token, locale: I18n.locale) %></p>
